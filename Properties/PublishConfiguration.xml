<Publish>
	<!--Id must be set in order to update existed mod. it is not required for publishing mod for the first time-->
	<ModId Value="150546" />
	<!--Name of the mod to display to players-->
	<DisplayName Value="TransitTimetables" />
	<!--Short description of the mod-->
	<ShortDescription Value="Real timetables for public transport — fixed departures, auto-sized fleets and terminus timing points. Works for buses, trams, trains, metros, ferries and planes." />
	<!--Long description of the mod. Single line or multi line. Supports minimal markdown subset-->
	<LongDescription>
Turn any public-transport line into a scheduled service. Works for every passenger type: buses, trams, trains, metros, ferries and aircraft.

Per line you opt in and set:
- a first departure time and peak / off-peak / night intervals (the peak windows are configurable),
- the mod then DERIVES how many vehicles the line needs to hold that headway, and raises the game's vehicle-count ceiling so the derived fleet is never clamped,
- a terminus stop (timing point) where early vehicles are held to the scheduled departure, and where surplus vehicles retire — after finishing their loop and dropping passengers, not mid-route.

Select any stop to open a printed-timetable departure board for every line that serves it, listed from the first departure of the day. Day-only and night-only lines only show and run the intervals that apply to them, and a night line is scheduled inside the night window.

No timetable set = the line runs exactly like vanilla. Everything is controlled from the line's own info panel plus a floating departures board.

OPTIONAL SETTINGS (all off by default — a plain timetable needs none of them):
- "Realistic travel time" (free): the game's own loop estimate is optimistic (it ignores the time buses lose braking and accelerating at every stop), so posted times drift ahead of reality. Turn this on and the mod measures each line's REAL loop as it runs and posts realistic stop times, so early buses wait at each stop until their true departure. It warms up — for the first day or two it still shows the estimate while it measures, then switches each stop to the measured time.
- "Provision fleet for real travel time" (costs money): also sizes each line's vehicle count to its real loop. A line whose real loop is ~2x the estimate needs about twice the vehicles to hold the same headway. Grow-only and capped.
- "Stop at every stop (even empty ones)" (free): by default a bus skips a stop where nobody boards or alights, so that stop isn't held to schedule; the terminus is now always honoured, and this extends that to every stop. Lines run a little slower.
- "Realistic Trips / slow-time compatibility" (ALPHA): keeps timing correct if you run a mod that lengthens the in-game day (Time2Work / Realistic Trips). Leave off otherwise.

Recommended: for accurate timetables at no cost, turn on "Realistic travel time" and leave the rest off; add "Provision fleet" only if you want the mod to also buy the buses that headway really needs.

Source (open source, MIT): https://github.com/AmicusDeus/TransitTimetables

Made with Claude Code's Opus 4.8 and Fable 5 models.
	</LongDescription>
	<!--Thumbnail-->
	<Thumbnail Value="Properties/Thumbnail.png" />
	<!--Screenshot, can be set multiple times-->
	<Screenshot Value="" />
	<!--Tag, can be set multiple times-->
	<Tag Value="" />
	<!--Link to the forum post where the mod can be discussed-->
	<ForumLink Value="" />
	<!--Version of the mod-->
	<ModVersion Value="0.3.1" />
	<!--Recommended version of the base game to use the mod-->
	<GameVersion Value="1.6.*" />
	<!--Dependency for the mod, can be set multiple times-->
	<Dependency Id="" />
	<!--Required DLC, can be set multiple times. Supported values are "Landmark Buildings", "San Fransisco Set", "Beach Properties", "Urban Promenades", "Modern Architecture"-->
	<RequiredDLC Value="" />
	<!--Change log for new version. Single line or multi line. Supports minimal markdown subset-->
	<ChangeLog>
v0.3.1 — Buses now always stop at the terminus (and optionally at every stop)

- FIX: a bus could SKIP a stop where nobody was boarding or alighting (base-game behaviour — the bus only slows and rolls through, never pulling in). A skipped stop is never held to its scheduled departure, so the bus ran ahead of its timetable — and this could even happen at the TERMINUS, skipping the scheduled wait that anchors the whole line. Timetabled lines now ALWAYS force a stop at the terminus, so the schedule is honoured. (Reported by a community member — thank you.)

- NEW: "Stop at every stop (even empty ones)" setting, OFF by default. Extends the forced stop to every stop on a timetabled line, so every posted departure is honoured — at the cost of a short dwell at each empty stop, which makes lines run a little slower. Buses and other road vehicles only; trains, trams, ships and planes already stop at every station in the base game.

v0.3.0 (alpha) — Realistic travel time + per-line custom peak windows

- NEW (ALPHA): "Realistic travel time". Cities: Skylines II's own estimate of how long a line takes is optimistic — it ignores the time buses lose accelerating and braking at every stop, so timetabled lines quietly run a longer effective headway than you set, and the vanilla "vehicles needed" count is too low. The mod now measures each line's REAL loop as it runs and corrects for it. Two new settings, BOTH OFF BY DEFAULT:
  • "Realistic travel time (correct posted times)" — posts realistic stop times so the departures board matches what the buses actually do. Free; it only makes the clock honest.
  • "Provision fleet for real travel time (costs money)" — also sizes each line's vehicle count to its real loop. This one spends: a line whose real loop is ~2x the estimate needs about twice the vehicles, and twice the upkeep. Grow-only and capped. OFF = your fleets are unchanged and the panel just tells you the real number needed.
  Self-calibrating per line, per city — no setup; a new line uses a stop-density estimate until it has measured itself. Works alongside slow-time mods (measured in sim-frames, so a stretched clock doesn't affect it). ALPHA — please report anything odd.

- NEW: per-line custom peak windows (thanks to community contributor incconutwo). A line can set its own two peak windows and headway — e.g. a school or commuter line that runs frequently only 07:00-09:00 and 16:00-18:00 — overriding the global peak settings, without affecting other lines.

- The "Realistic Trips / slow-time compatibility" setting now defaults OFF (turn it ON only if you run Time2Work / Realistic Trips); the mod logs a note if it detects such a mod while the setting is off.

Earlier: v0.2.83 added station terminus points, per-row "Set as terminus" buttons, and the "Keep achievements enabled" toggle; v0.2.7 fixed the vanilla "Assigned Vehicles" slider; v0.2.3 restored on-time departures.
	</ChangeLog>
	<!--External link, can be set multiple times. supported types are "discord", "github", "youtube", "twitch", "x", "paypal", "patreon", "buymeacoffee", "kofi", "crowdin", "gitlab", "gofundme"-->
	<ExternalLink Type="github" Url="https://github.com/AmicusDeus/TransitTimetables" />
	<!--Supported values Public, Private, Unlisted-->
	<AccessLevel Value="Public" />
</Publish>
